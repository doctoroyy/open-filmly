name: Flutter CI

on:
  push:
    branches: ['**']
    paths:
      - 'app/**'
      - '.github/workflows/**'
  pull_request:
    paths:
      - 'app/**'
      - '.github/workflows/**'

concurrency:
  group: flutter-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FLUTTER_VERSION: '3.44.0'

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests
        run: |
          find test -maxdepth 1 -name '*_test.dart' \
            ! -name 'integration_smb_real_test.dart' \
            ! -name 'ui_automation_test.dart' \
            -print0 | sort -z | xargs -0 flutter test

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    needs: analyze-and-test
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Install CocoaPods dependencies
        run: cd macos && pod install

      - name: Build macOS (release, no codesign)
        run: flutter build macos --release

      - name: Package DMG
        run: scripts/make_dmg.sh build/Open-Filmly.dmg

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: open-filmly-macos-dmg
          path: app/build/Open-Filmly.dmg
          if-no-files-found: error

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    needs: analyze-and-test
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows
        run: flutter build windows --release

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: open-filmly-windows-release
          path: app/build/windows/x64/runner/Release/**
          if-no-files-found: error
